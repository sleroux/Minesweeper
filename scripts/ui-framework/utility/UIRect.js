define(function () {
    'use strict';

    var UIRect = function (x, y, width, height) {
        return {
            x: x,
            y: y,
            width: width,
            height: height
        };
    };

    return UIRect;
});